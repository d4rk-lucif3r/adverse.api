# Example environment file - DO NOT store actual secrets here
# Copy this file to .env and add your actual secrets there
# .env should be in .gitignore to prevent committing secrets

# MongoDB connection string (replace with your actual database connection string)
MONGODB_URI=mongodb://username:password@host:port/database

# API keys should be stored as environment variables, not in code or data files
API_KEY=your_api_key_here

# Other configuration
DEBUG=false
LOG_LEVEL=info